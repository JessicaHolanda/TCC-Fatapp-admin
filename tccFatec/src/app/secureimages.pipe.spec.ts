import { SecureimagesPipe } from './secureimages.pipe';

describe('SecureimagesPipe', () => {
  it('create an instance', () => {
    const pipe = new SecureimagesPipe();
    expect(pipe).toBeTruthy();
  });
});
